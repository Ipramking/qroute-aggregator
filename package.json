{
  "name": "qroute-aggregator-root",
  "private": true,
  "workspaces": [
    "contracts",
    "routing-engine",
    "frontend",
    "arbitrage-bot"
  ],
  "scripts": {
    "build": "npm run build -w routing-engine && npm run build -w frontend",
    "test": "npm run test -w contracts && npm run test -w routing-engine && npm run test -w arbitrage-bot"
  }
}
